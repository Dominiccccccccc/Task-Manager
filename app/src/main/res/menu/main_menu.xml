<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single">
        <item android:id="@+id/home"
            android:icon="@drawable/ic_baseline_home_24"
            android:title="Home"
            />
        <item android:id="@+id/create"
            android:icon="@drawable/ic_create_task"
            android:title="Create Task"/>
        <item android:id="@+id/history"
            android:icon="@drawable/ic_history"
            android:title="Task History"/>
    </group>

    <item android:title="Additional Functionalities">
        <menu>
            <group android:checkableBehavior="single">

                <item android:id="@+id/pomodoro"
                    android:icon="@drawable/ic_pomodoro_timer"
                    android:title="Start Focus Session"/>
                <item android:id="@+id/weather"
                    android:icon="@drawable/ic_cloud"
                    android:title="Check Weather"/>
                <item android:id="@+id/homeWidget"
                    android:icon="@drawable/ic_home_widget"
                    android:title="Add to Home Widget"/>

            </group>
        </menu>
    </item>
</menu>